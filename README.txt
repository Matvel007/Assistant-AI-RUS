Голосовой ассистент Мэй с Live2D-аватаром
============================================

Проект состоит из двух основных частей:
1. Локальный голосовой ассистент с графическим интерфейсом, RAG-памятью,
   поиском в интернете, голосовым вводом (GigaAM) и синтезом речи (Silero TTS).
2. Live2D-аватар на базе Open-LLM-VTuber, который подключается к ассистенту
   через локальный API и может отображаться в виде прозрачного окна
   («пет-режим»), всегда доступного на рабочем столе.

Всё работает локально, поддерживается GPU (NVIDIA CUDA) для ускорения ASR,
TTS и эмбеддингов.

------------------------------------------------------------
Системные требования
------------------------------------------------------------
- Операционная система: Windows 10/11 (64-bit)
- Python: 3.10 или 3.11 (рекомендуется 3.11)
- ОЗУ: минимум 8 ГБ (рекомендуется 16 ГБ)
- Видеокарта: NVIDIA с поддержкой CUDA (опционально, для GPU-ускорения).
  Если карты нет, всё будет работать на CPU, но медленнее.
- Место на диске: около 4–8 ГБ (модели загружаются в кеш при первом запуске)

------------------------------------------------------------
Установка
------------------------------------------------------------

1. Распакуйте архив с проектом в удобную папку, например:
   C:\Assistant-Mae

2. Установите Python 3.11 (если не установлен) с официального сайта
   https://www.python.org/downloads/. При установке обязательно отметьте
   галочку «Add Python to PATH».

3. Откройте командную строку (Win+R → cmd) и перейдите в папку проекта:
   cd /d C:\Assistant-Mae

4. (Рекомендуется) Создайте виртуальное окружение и активируйте его:
   python -m venv venv
   venv\Scripts\activate

5. Установите все зависимости:
   pip install -r requirements.txt

   Если возникнут ошибки с пакетом onnx-asr, попробуйте установить его отдельно:
   pip install onnx-asr[gpu]   (или [cpu] для работы без видеокарты)

6. Настройте API-ключи в файле config.yaml (откройте в блокноте):
   - Найдите секцию tools.tavily и вставьте свой ключ от Tavily (если нужен поиск).
   - В секции tools.deepseek укажите свой ключ от DeepSeek (можно получить на
     platform.deepseek.com) в поле api_key_official. Если используете AITUNNEL,
     измените provider на aitunnel и заполните соответствующий ключ.
   - При желании измените имя ассистента (assistant_name) и другие параметры.

7. Настройте конфиг аватара Open-LLM-VTuber/conf.yaml (обычно трогать не нужно,
   всё уже настроено). Убедитесь, что в секции openai_compatible_llm указан
   правильный адрес API: http://localhost:8001/v1.

------------------------------------------------------------
Запуск
------------------------------------------------------------

Вариант 1: Только ассистент (графический интерфейс)
----------------------------------------------------
В командной строке (из папки проекта) выполните:
   python main.py

Откроется окно с чатом. Кнопка микрофона включает запись – говорите,
после паузы фраза отправится ассистенту, и вы услышите ответ.
Повторное нажатие кнопки отключает микрофон.

Вариант 2: Ассистент + Live2D-аватар
-------------------------------------
Для запуска всей системы используйте bat-файл:
   run_all.bat

или вручную:
   cd Open-LLM-VTuber
   python launcher.py

После запуска откроется окно с аватаром в пет-режиме (прозрачное, без рамки,
не поверх всех окон). Теперь можно общаться голосом через аватар:
- Нажмите Alt+X (глобальная горячая клавиша) – микрофон включится.
- Говорите.
- Нажмите Alt+X ещё раз – запись остановится и отправится ассистенту.
Аватар озвучит ответ голосом Silero.

------------------------------------------------------------
Особенности и управление
------------------------------------------------------------

- В основном ассистенте микрофон включается кнопкой «🎤 Вкл». После паузы
  (около 1 секунды тишины) запись автоматически распознаётся и отправляется,
  микрофон отключается. Чтобы прервать запись, нажмите кнопку ещё раз.

- В аватаре микрофон переключается глобальной клавишей Alt+X (работает даже
  когда окно аватара не в фокусе). В интерфейсе есть кнопка, дублирующая это
  действие.

- Модели скачиваются автоматически при первом использовании:
  * Эмбеддинги BAAI/bge-m3 (~2 ГБ)
  * GigaAM для ASR (~1 ГБ) – в кеш Hugging Face
  * Silero VAD (небольшая)
  * Silero TTS (~100 МБ)

- База данных памяти (ChromaDB) хранится в папке data/chroma_db.

- При использовании GPU следите за объёмом видеопамяти. В случае ошибок
  «out of memory» переключите в конфигах device: 'cpu'.

------------------------------------------------------------
Возможные проблемы и решения
------------------------------------------------------------

Ошибка при установке onnx-asr
------------------------------
Если возникает ошибка компиляции, попробуйте установить предварительно
собранные колеса из папки packages (если они есть в поставке):
   pip install --no-index --find-links=packages onnx-asr[gpu]
Или установите onnx-asr без GPU:
   pip install onnx-asr[cpu]

Микрофон не работает или не распознаётся
----------------------------------------
- Проверьте, что микрофон подключён и выбран устройством ввода по умолчанию
  в Windows.
- Если возникают ошибки с pyaudio, установите его вручную, скачав колесо с
  https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio для вашей версии Python.

Аватар не переключается в пет-режим
------------------------------------
Убедитесь, что в Open-LLM-VTuber/conf.yaml присутствует секция:
   default_mode: 'pet'
   pet:
     always_on_top: false
     click_through: true
     position: 'bottom_right'
Если окно всё равно поверх всех окон, значит, в исходниках не удалён вызов
setAlwaysOnTop. В готовом .exe этот флаг уже отключён.

Ошибки, связанные с CUDA / out of memory
-----------------------------------------
Уменьшите размер модели или используйте CPU. В конфигах (config.yaml и conf.yaml)
поменяйте device: 'cuda' на 'cpu'.

Не распознаётся речь (GigaAM выдаёт странный текст)
----------------------------------------------------
Убедитесь, что в config.yaml секция asr настроена правильно:
   asr:
     enabled: true
     model_name: 'gigaam-v3-e2e-ctc'
     device: 'cuda'
При первом запуске модель скачается в кеш. Если качество плохое, попробуйте
настроить VAD (порог чувствительности в коде).

------------------------------------------------------------
Благодарности и ссылки
------------------------------------------------------------
- Open-LLM-VTuber: https://github.com/t41372/Open-LLM-VTuber
- Silero Models: https://github.com/snakers4/silero-models
- GigaAM: https://github.com/salute-developers/GigaAM
- ChromaDB: https://www.trychroma.com/

------------------------------------------------------------
Приятного общения с Мэй!